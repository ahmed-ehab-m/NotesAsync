import 'package:flutter/material.dart';

const kPrimaryColor = Colors.orange;

const kSecondaryColor = Colors.grey;
const kNotesBox = 'notes_box';
List<Color> kColors = const [
  // Color.fromRGBO(238, 238, 238, 1),
  Color(0x1A9E9E9E),
  Color(0xCCDBCF96),
  Color(0xCC9B1D20),
  Color(0xCC8B5FBF),
  Color(0xCC6665DD),
  Color(0xCC6AB547),
];
// Color softenColor(Color color, double factor) {
//   HSLColor hslColor = HSLColor.fromColor(color);
//   HSLColor softened = hslColor.withLightness(hslColor.lightness * factor);
//   return softened.toColor();
// }

const int KsFontSize = 10;
const int KmFontSize = 14;
const int KlFontSize = 18;
const int KhFontSize = 20;
